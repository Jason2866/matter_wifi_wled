[pytest]
testpaths = tests
markers =
    bridge: tests that only need the bridge HTTP API
    wled: tests that also query WLED devices directly
    matter: tests that require chip-tool (skipped if unavailable)
    destructive: tests that modify device config (require --run-destructive)
